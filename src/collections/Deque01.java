package collections;

public class Deque01 {
    /**
     * Deque 는 Queue의 변형으로, 한 쪽 끝으로만 추가/삭제할 수 있는 Queue와 달리, Deque는 양쪽 끝에 추가/삭제가 가능.
     * 구현체로는 ArrayDeque와 LinkedList 등이 있음
     *
     * offerLast() : 끝에 저장
     * pollLast() : 끝에서 삭제
     * offerFirst() : 앞에서 저장
     * pollFirst() : 앞에서 삭제
     *
     * peekLast() : Stack의 peek()과 동일, 마지막 값을 꺼내어 반환하지만, 실제 디큐에서 삭제하진 않음.
     */
}
